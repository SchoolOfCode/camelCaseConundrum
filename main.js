/* 

Write a Function that takes in a string containing any number of words, and convert them into a single word in valid camelCase.
e.g. 'make me camel case' returns 'makeMeCamelCase'.
If the string is empty, or contains only one word, your function should return null.
e.g. '' or 'Hi' returns null.

The input string will only consist of uppercase letters, lowercase letters and spaces.

*/

export default function covertToCamelCase() {
	// Write your code here!!
}
